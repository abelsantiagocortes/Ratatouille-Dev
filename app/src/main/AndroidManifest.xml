<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ratatouille">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Chef.ChefRecipes"></activity>
        <activity android:name=".Client.ClientRequest" />
        <activity android:name=".Chef.ChefProfile" />
        <activity android:name=".Chef.DescripcionChef" />
        <activity android:name=".Chef.ChefActivity" />
        <activity android:name=".ClientChef.ToolsRegister" />
        <activity android:name=".ClientChef.ClientChefRegister" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".Client.Home" />
        <activity android:name=".Client.Welcome" />
        <activity android:name=".Chef.ChefRegister" />
        <activity android:name=".Client.ClientRegister" />
        <activity android:name=".ClientChef.Register" />
        <activity android:name=".ClientChef.LogIn" />
        <activity android:name=".ClientChef.LandingPage" />
        <activity android:name=".ClientChef.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Chef.Chefs_tab" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>